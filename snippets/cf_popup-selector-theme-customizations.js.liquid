/**
 * Update the classes below to customize areas of the popup selector
*/

/**
 * These variables are declared in cf_popup_selector.js.liquid
 */
popupFormClasses = '';
popupTitleClass = 'h4';
popupSubtitleClass = '';
popupVariantImageClasses = 'width-200';
popupSubmitButtonClasses = '';
popupCancelButtonClasses = '';
